# 第十五階段－議題列表廠商與搜尋－技術可行性評估

**角色：** RD Tech Spike  
**日期：** 2026-09-09  

可行。`ListAsync` 加 `ClientCompanyId` 與 `IssueNo`／`Title` 的 `ToLower().Contains`，與 `DirectoryService` 搜尋相同。廠商清單沿用 `GET /client-companies`。無 DB 變更。
