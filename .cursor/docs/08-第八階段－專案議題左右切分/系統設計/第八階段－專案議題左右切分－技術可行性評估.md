# 第八階段－專案議題左右切分－技術可行性評估

**角色：** RD Tech Spike  
**日期：** 2026-09-04  

| 項目 | 結論 | 說明 |
|------|------|------|
| 取代 dialog | 可行 | 同一份 `itemForm` 改掛右側；刪除確認仍 `AppDialog`。 |
| 主從欄 | 可行 | 對齊 `.todo-workspace`／`.todo-detail-col`；窄螢幕既有 860px 改單欄。 |
| 追蹤 TODO | 可行 | 移出 dialog、放在項次 `<form>` **外面**，避免 Enter 誤存項次（第五階段已遇過）。 |
| 跳轉 | 可行 | 既有 `openEdit`；不再 `showModal`。 |
| API／DB | 不需改 | |

**已確認可行。**
