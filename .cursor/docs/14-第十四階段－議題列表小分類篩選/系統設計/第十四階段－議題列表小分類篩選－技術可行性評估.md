# 第十四階段－議題列表小分類篩選－技術可行性評估

**角色：** RD Tech Spike  
**日期：** 2026-09-09  

可行。`IssueService.ListAsync` 加 `Where(x => x.SubCategoryId == subCategoryId)`。Chip 沿用既有樣式，資料改呼叫既有 `GET /subCategories`。`majorCategoryId` 保留給首頁。無 DB 變更。
