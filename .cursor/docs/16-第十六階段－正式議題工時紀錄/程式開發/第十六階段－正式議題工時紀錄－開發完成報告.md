# 第十六階段－正式議題工時紀錄－開發完成報告

**角色：** RD  
**日期：** 2026-09-13  
**依據：** [系統設計書](../系統設計/第十六階段－正式議題工時紀錄－系統設計書.md) v1.0  

`work_hour` 新增可空 `issue_id`（三選一 CHECK、唯一鍵、FK）。`WorkHourService` 支援正式議題 owner；`IssuesController` 提供 `/issues/{id}/hours` CRUD。刪議題時一併刪工時。`IssuePage` 複用 `WorkHourTable`，合計由前端加總。不做完成／實際日、Excel 解析、列表合計。

修改：`backend/Entities/WorkHour.cs`、`backend/Data/AppDbContext.cs`、`backend/Data/DbSeeder.MySql.cs`、`backend/Data/DbSeeder.SqlServer.cs`、`backend/Services/WorkHourService.cs`、`backend/Services/IssueService.cs`、`backend/Controllers/IssuesController.cs`、`backend.Tests/FormalIssueHourTests.cs`、`frontend/src/api.js`、`frontend/src/pages/IssuePage.jsx`、`frontend/src/components/WorkHourTable.jsx`。

後端建置通過（既有 EF1002 seeder 警告不變）。`FormalIssueHourTests` 3 項通過。後續補：日期預設當天；工時錯誤就近顯示於卡片內。
