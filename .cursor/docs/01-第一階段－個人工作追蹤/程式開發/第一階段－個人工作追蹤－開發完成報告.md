# 第一階段－個人工作追蹤－開發完成報告

**角色：** RD  
**日期：** 2026-08-31  
**依據：** 系統設計書 v1.0、資料庫設計書 v1.0

## 本次完成功能

- 分類：固定四大分類種子、小分類 CRUD、同名檢查、使用中不可刪
- 正式議題 CRUD（標題、內容、大小分類、預計完成日、備註）
- 多層 TODO：新增／編輯／刪除（含子樹）、完成級聯、同層拖曳排序（拒絕改掛）
- 首頁議題清單（大分類篩選）與月曆骨架
- 單人無登入

## 修改／新增檔案

- `backend/` ASP.NET Core 9 API（Controller、EF Core Pomelo、服務層）
- `backend.Tests/` 單元測試（InMemory）
- `frontend/` React + Vite 首頁、議題詳情、分類設定、TODO 樹、月曆
- `Issue.sln`

未新增獨立 Migration 專案；啟動時 `EnsureCreated` 建表並種子。

## API

見系統設計書第四章。基底 `http://localhost:5080`。

## 資料表

`major_category`、`sub_category`、`issue`、`issue_todo`（庫名 `issue_tracker`）

## 已完成／未完成

| 項目 | 狀態 |
|------|------|
| 規格本輪 MVP | 已實作 |
| 單元測試（自動完成、取消不上修、拒改掛、手動完成父項） | 4 項通過 |
| 連線本機 MySQL 並做瀏覽器手動驗收 | 視 `appsettings.json` 帳密而定，RD 未探測 root 密碼 |
| 登入、專案、Excel、預約、加簽恢復 | 本輪不做 |

## 已知限制

- MySQL 連線字串預設 `root/root`，請依本機修改 `backend/appsettings.json`。
- 無 Docker。
- TODO 編輯使用瀏覽器 `prompt`，非獨立表單。
- 本輪無 HTTPS 導向，方便本機 HTTP SPA。

## 建議 QA 測試重點

- 四大分類顏色與不可刪
- 小分類重名、刪除使用中
- 議題完成＝改大分類「已完成」
- 三層 TODO 最後一筆勾選後級聯完成；取消子項不取消父項
- 拖曳僅同層；月曆只顯示有預計完成日的議題
