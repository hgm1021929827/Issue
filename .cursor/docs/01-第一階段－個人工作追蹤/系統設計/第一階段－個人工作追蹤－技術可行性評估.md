# 第一階段－個人工作追蹤－技術可行性評估

**角色：** RD（Tech Spike，非正式開發）  
**日期：** 2026-08-31  
**依據：** 規格書第八章待評估項、系統設計草擬 API／UI

## 結論

**可行。** 建議採 React（Vite）SPA + ASP.NET Core 9 API Controller + MySQL 8（EF Core Pomelo）。不採用 Razor 混出產品畫面，以免與 React 狀態重複。

本輪無須交付正式功能程式於 Spike 階段；下列為評估結果，供 SA 定稿。

## 評估項目

| 項目 | 結果 | 說明 |
|------|------|------|
| 前後端切分 | 可行 | .NET 9 與 Node 22 已在本機可用。MVC 專案可只掛 API Controller。 |
| 多層 TODO 樹 | 可行 | 遞迴元件 + `parentId`；完成級聯放後端，避免前後端各算一套。 |
| 同層拖曳 | 可行 | `@dnd-kit/core` + `@dnd-kit/sortable`，每個父節點一個 SortableContext。跨父項 drop 忽略。 |
| 月曆骨架 | 可行 | 7×n CSS Grid 自製即可；標記為色塊＋截斷標題。不必上 FullCalendar。 |
| 色票 | 可行 | 大分類 `color` 當 CSS 背景；規格四色對比足夠白字。 |
| MySQL | 可行 | 本機已有 MySQL80 服務。無 Docker。連線字串由 `appsettings.Development.json` 設定，不在 Spike 探測 root 密碼。 |

## 框架成本

| 工作 | 成本 |
|------|------|
| 專案骨架、CORS、統一 Resp | 低 |
| 分類／議題 CRUD | 低 |
| TODO 樹＋自動完成＋測試 | 中 |
| 月曆 | 低 |
| 拖曳 | 中 |

## 規格回饋

- 8.3 行事曆、8.5 TODO 樹：由「待評估」改為**已確認可行**。
- 不調整產品範圍。
