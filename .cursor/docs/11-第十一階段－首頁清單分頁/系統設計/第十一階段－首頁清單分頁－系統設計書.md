# 第十一階段－首頁清單分頁－系統設計書

**角色：** SA  
**版本：** v1.0  
**日期：** 2026-09-04  
**依據：** [功能規格書](../需求規劃/第十一階段－首頁清單分頁－功能規格書.md) v1.0  

# 一～二

只改 `HomePage` 與必要 CSS。後端不改。`/issues`、`/projects` 獨立頁維持。

# 三、流程

- 版面恢復 `.page-grid`：左清單、右月曆。
- Tab：`projects`／`issues`／`tracks`。預設專案。不寫進 URL。
- 專案：`GET /projects` 後對每筆 `GET /projects/{id}/items`，列下嵌套項次；點專案進 `/projects/{id}`，點項次進 `?item=`。
- 正式議題：分類 Chip；列＋`plan-btn` 拖曳／點選排入。拿掉第十階段月曆上方 `AppSelect`。
- 追蹤：沿用 `GET /track-todos`；列點進既有跳轉。無分類 Chip。
- 分類 Chip 在專案／正式議題 tab 共用，同時篩這兩份清單。

# 四、API

無變更。沿用既有專案、項次、議題、追蹤、行事曆、預計項目端點。

# 五～六

互動對應既有 GET／POST／DELETE。切 tab 不打新 API。

# 七、邏輯模型

無新 Entity。

## 7.4 DA 接手

無 Database。N/A。

# 八～十二

單人。無新例外。
