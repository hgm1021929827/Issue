# 第三階段－專案與專案議題－QA測試報告

**角色：** QA  
**日期：** 2026-09-02  
**驗收模式：** 需求類型 ① 功能驗收（A 路徑）  
**結案：** 使用者確認第三階段測試完成

## 驗收對象

| 文件 | 版本 |
|------|------|
| 系統設計書 | v1.0 |
| 資料庫設計書 | v1.0 |
| 開發完成報告 | 2026-09-02 |
| 已知限制 | 項次為字串序；同日標記格內捲動、無 +N 彈層；單人無登入 |

未讀《功能規格書》（需求類型 ①）。

## 執行結果

| 項目 | 結果 | 說明 |
|------|------|------|
| TC-01 單元測試 | 通過 | `dotnet test`：失敗 0，通過 29，略過 0 |
| TC-02／03 專案列表與篩選 | 通過 | GET `/projects` 回傳 P3-QA、`itemCount: 1`；`majorCategoryId=5`（議題）為 `[]` |
| TC-04 空白代號 | 通過 | POST `code` 僅空白 → HTTP 400 |
| TC-05 代號忽略大小寫 | 通過 | POST `p3-qa` → HTTP 409 `專案代號已存在` |
| TC-06 起迄顛倒 | 通過 | HTTP 400 `預計開始日不可晚於預計完成日` |
| TC-07／08 詳情與 404 | 通過 | GET `/projects/1` 含 `items`；GET `/projects/99999` → 404 `找不到該專案` |
| TC-09／10 項次 | 通過 | 同專案重複項次 409 `同一專案已有相同項次`；寫入成功回整份列表 |
| TC-11 路徑不符 | 通過 | PUT `/projects/999/items/1` → 404 |
| TC-12 行事曆增量 | 通過 | `source=project`、`kind=due`、`label=P3-QA`、日期 2026-09-30；無專案 `plan`；無 9/1 開始日列。單元測試另覆蓋專案議題 due 與不含 start |
| TC-13 月曆跳轉 | 抽樣通過 | 標記 `aria-label`「專案議題 P3-QA 1」「專案 P3-QA」；`?item=1` 打開編輯；`?item=999` 橫幅「找不到該專案議題」。React key 含 `source` |
| TC-14 分類刪除 | 通過 | DELETE `/majorCategories/3`（預約，有專案與專案議題）→ 409 `使用中，共 1 筆專案、1 筆專案議題`。單元測試覆蓋小分類被專案／專案議題引用 |
| TC-16 刪專案連刪列 | 通過（單元） | `Delete_project_removes_items`：`itemCount` 正確且兩表清空 |
| TC-17 首頁清單 | 通過 | 議題清單僅正式議題；導覽另有「專案」 |
| TC-18 Schema | 通過 | `DbSeeder.EnsureSchema` 以 `CREATE TABLE IF NOT EXISTS` 建 `project`、`project_issue`；刪專案 CASCADE、分類 RESTRICT |
| 前端手動清單 | 通過 | 使用者於 2026-09-02 確認完成（含議題新增／編輯改 `dialog`、下拉不撐開捲軸、標題列與第一列間距） |

## 發現問題與分派

尚無未結 Bug。本輪不開立《QA問題確認文件》。

已知限制（開發完成報告，不列為 Bug）：項次字串排序（「10」在「2」前）；同日擁擠只捲動。

## 結論

**可結案。** 第三階段功能驗收完成。單元測試 29 項通過；前端手動測試由使用者確認完成。後續產品調整（Excel 匯入、臨時工作、工時等）請開下一階段，不覆寫本階段正式文件。
