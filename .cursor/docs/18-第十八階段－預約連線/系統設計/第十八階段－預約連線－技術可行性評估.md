# 第十八階段－預約連線－技術可行性評估

**角色：** SA（Tech Spike）  
**日期：** 2026-09-18  

| 項目 | 結論 |
|------|------|
| `issue_todo` 第三承載 `project_id` | 可行；與項次相同 XOR CHECK |
| SQL Server 專案 FK CASCADE | 經 work_item 可能多重路徑；專案／項次 FK Restrict，刪除走應用層。MySQL 專案列可 CASCADE |
| 預約兩表 | 可行；事項多態對齊 track_todo 恰一 FK |
| 合併提示 | 前端送出前 GET future；不需後端強制（允許明確再新增） |
| 行事曆增量 | 既有 `CalendarAsync` 加來源；新視角 chip |
| 狀態掛「預約」名稱 | 可行；改名後找不到則表單擋下，不寫死 ID |
| 處理事項挑選 | 議題／專案清單＋待辦樹扁平選項即可，不必新搜尋 API |

建議：本輪實作，無阻擋。
