# 第十七階段－工作項次待辦－技術可行性評估

**角色：** SA（Tech Spike）  
**日期：** 2026-09-17  

| 項目 | 結論 |
|------|------|
| 擴充 `issue_todo` 第二承載 | 可行；對齊 `track_todo` XOR CHECK |
| 共用 `/todos/{id}` | 可行；同一 IDENTITY，不另開表 |
| 工作項次右欄再套雙欄待辦 | 過窄；用 `compact` 單欄 |
| SQL Server FK CASCADE | 經 project → work_item 可能多重路徑；FK Restrict，刪除走應用層 |
| 新增表單加內容 | 改 `TodoComposer`；議題頁一併變更 |

建議：本輪實作，無阻擋。
