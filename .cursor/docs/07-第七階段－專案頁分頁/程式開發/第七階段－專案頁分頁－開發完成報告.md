# 第七階段－專案頁分頁－開發完成報告

**角色：** RD  
**日期：** 2026-09-04  
**狀態：** 開發完成，交 QA  
**依據：** [系統設計書](../系統設計/第七階段－專案頁分頁－系統設計書.md) v1.0、[資料庫設計書](../資料設計/第七階段－專案頁分頁－資料庫設計書.md) v1.0

---

## 本次完成功能

- 已儲存專案詳情：標題下兩個 chip tab「專案內容」「專案議題（N）」
- 專案內容：既有表單、儲存／刪除、專案層追蹤 TODO
- 專案議題：列表與新增；編輯仍為 dialog
- 新增專案頁無 tab
- `?item=` 進入議題 tab 並高亮／開編輯；僅 `trackTodo` 留在內容 tab
- 切 tab 不重置未存的專案表單 state；dialog 開著時切 tab 不強制關掉

## 資料庫

無變更。

## API

無變更。

## 修改檔案

- `frontend/src/pages/ProjectPage.jsx`
- `frontend/src/styles.css`（`.page-title + .filters` 間距）

## 單元測試

本輪無後端增量。回歸：`dotnet test` 應維持既有通過數。

## 已知限制

- tab 不寫進 URL（除既有 `item`／`trackTodo`）。
- 不寫 `localStorage`。

## 建議 QA 測試重點

1. 新增專案無 tab；儲存後才有。
2. 兩 tab 互斥顯示；議題 tab 有完整列表空間。
3. 內容 tab 可儲存專案與操作專案追蹤。
4. 議題 CRUD、dialog 內項次追蹤仍可用。
5. 月曆／首頁 `?item=` 落到議題 tab；專案層 `trackTodo` 落到內容 tab。
6. 找不到 `item`：橫幅＋專案仍開。
7. 視覺：chip 與成員頁同類，粉彩 token。
